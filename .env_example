# MyBlog Environment Configuration Template
# このファイルをコピーして .env を作成し、実際の値を入力してください
# または scripts/sync-env.sh を実行して自動生成してください
#
# Setup:
#   cp .env_example .env
#   vi .env  # パスワードを設定
#
# Auto-sync from CDK:
#   chmod +x scripts/sync-env.sh
#   ./scripts/sync-env.sh

# ==============================================
# AWS Configuration
# ==============================================
AWS_PROFILE=myblog-dev
AWS_REGION=ap-northeast-1

# ==============================================
# API Configuration
# ==============================================
# API Gateway エンドポイント（CDKデプロイ後に自動設定）
API_URL=https://xxxxxxxxxx.execute-api.ap-northeast-1.amazonaws.com/prod

# ==============================================
# Cognito Configuration
# ==============================================
# Cognito User Pool ID（CDKデプロイ後に自動設定）
COGNITO_USER_POOL_ID=ap-northeast-1_XXXXXXXXX

# Cognito User Pool Client ID（CDKデプロイ後に自動設定）
COGNITO_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# ==============================================
# DynamoDB Configuration
# ==============================================
# DynamoDB Table Name（CDKデプロイ後に自動設定）
DYNAMODB_TABLE_NAME=MyBlog-DataStack-MyBlogTable-XXXXX

# ==============================================
# S3 & CloudFront Configuration
# ==============================================
# Media Bucket（ユーザーアップロードコンテンツ用）
MEDIA_BUCKET_NAME=myblog-datastack-mediabucket-xxxxx
MEDIA_CLOUDFRONT_DOMAIN=xxxxxx.cloudfront.net

# Frontend Bucket（Webサイトホスティング用）
FRONTEND_BUCKET_NAME=myblog-appstack-frontendbucket-xxxxx
FRONTEND_CLOUDFRONT_DOMAIN=xxxxxx.cloudfront.net

# ==============================================
# Test User Credentials
# ==============================================
# テストユーザーのメールアドレス
TEST_USER_EMAIL=your-email@example.com

# テストユーザーのパスワード
# ⚠️ 重要: このファイルをGitにコミットしないこと！
TEST_USER_PASSWORD=YourSecurePassword123!

# ==============================================
# Optional: Manual JWT Token
# ==============================================
# 手動で取得したJWTトークンを設定する場合
# 自動ログインを使わない場合のみ設定
# JWT_TOKEN=eyJhbGciOiJSUzI1NiIs...

# ==============================================
# Environment
# ==============================================
NODE_ENV=development

# ==============================================
# Notes
# ==============================================
# JWT Token の有効期限: 1時間
# - IDトークン: 1時間
# - アクセストークン: 1時間  
# - リフレッシュトークン: 30日
#
# リソース名の自動同期:
# CDKスタックをデプロイ/更新した後は必ず以下を実行:
#   ./scripts/sync-env.sh
#
# これにより、DynamoDBテーブル名やS3バケット名などが自動更新されます。
